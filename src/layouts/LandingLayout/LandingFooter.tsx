import styled from "styled-components";

const Wrapper = styled.header`
  min-height: 5rem;
  background-color: var(--NEUTRAL-2);
`;
const LandingFooter = () => {
  return <Wrapper>LandingFooter</Wrapper>;
};

export default LandingFooter;
